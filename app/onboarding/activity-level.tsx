import { ScreenStub } from '@/components/ScreenStub';

export default function ActivityLevel() {
  return <ScreenStub label="activity-level" next={{ to: '/onboarding/diet', label: 'Continue' }} />;
}
