import { ScreenStub } from '@/components/ScreenStub';

export default function PlanReveal() {
  return <ScreenStub label="plan-reveal" next={{ to: '/onboarding/permissions', label: 'Continue' }} />;
}
